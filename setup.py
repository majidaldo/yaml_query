from setuptools import setup

setup(name='yaml_query',
      version='0.1',
      description='Execute SQL queries on YAML file',
      url='http://github.com/majidaldo/yaml_query',
      author='Majid alDosari',
      author_email='majidaldosari-github@githubgithub.com',
      license='MIT',
      packages=['yaml_query'],
      install_requires=['pyyaml'],
      zip_safe=True)
